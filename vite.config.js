import { defineConfig } from 'vite'

export default defineConfig({
  // For GitHub Pages project repo (username.github.io/portfolio),
  // change base to '/portfolio/' or set via VITE_BASE env var.
  // For a custom domain or user root site, keep './'.
  base: './',
})
